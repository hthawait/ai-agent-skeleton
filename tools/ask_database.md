# PostgreSQL Database

Use this tool for questions that require querying the configured PostgreSQL
database. It translates the user's question into SQL, inspects the schema,
executes a read-only query, and returns an answer based on the result.

Use it for counts, filtering, aggregation, comparisons, and other questions
about database records. Do not use it for general knowledge or arithmetic.